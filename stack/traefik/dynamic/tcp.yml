tcp:
  routers:
    mtproxy:
      rule: "HostSNI(`*`)"
      entryPoints:
        - websecure
      service: mtproxy
      tls:
        passthrough: true
  services:
    mtproxy:
      loadBalancer:
        proxyProtocol:
          version: 2
        servers:
          - address: "telemt:1234"
